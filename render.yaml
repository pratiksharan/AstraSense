services:
  - type: web
    name: astrasense-command
    env: node
    plan: free
    buildCommand: npm install; npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
